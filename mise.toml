[tools]
typst = "0.15.1"
typstyle = "0.15.1"
tinymist = "0.15.2"

[tasks.test]
description = "Run the Tinymist test suite"
run = "tinymist test tests/main.typ --root . --no-dashboard --coverage --print-coverage=full"
